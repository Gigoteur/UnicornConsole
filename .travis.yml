dist: xenial

language: rust

rust:
  - stable
  - nightly

jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true

addons:
  apt:
    update: true

cache: cargo

script: cd unicorn && cargo build --release