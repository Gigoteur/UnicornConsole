dist: xenial

language: rust

rust:
  - stable
  - nightly

jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true
  include:
    - name: "Unicorn Core"
      script: cd unicorn && cargo build --release


addons:
  apt:
    update: true

cache: cargo
